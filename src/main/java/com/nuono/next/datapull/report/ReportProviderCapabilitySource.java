package com.nuono.next.datapull.report;

/** Typed release evidence exposed by a production report provider bridge. */
public interface ReportProviderCapabilitySource {

    ReportProviderCapabilities reportProviderCapabilities();
}
