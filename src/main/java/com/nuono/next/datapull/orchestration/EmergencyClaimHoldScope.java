package com.nuono.next.datapull.orchestration;

/** Technical blast radius for an emergency stop on new DP task claims. */
public enum EmergencyClaimHoldScope {
    GLOBAL,
    OPERATION,
    SCOPE
}
