package com.nuono.next.datapull.orchestration;

/** Deep Interface combining hold recording with dispatch-time lookup. */
public interface BackoffHoldStore extends BackoffHoldGate, BackoffHoldRecorder {
}
